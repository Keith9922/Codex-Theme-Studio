# Codex Theme Studio Landing Page

产品落地页，集中说明 App 与 `create-codex-theme` Skill 的安装、生成、识别、多主题切换流程，以及必要的安全、隐私、签名和素材权利声明。

## 本地运行

```bash
npm install
npm run dev
```

## 校验

```bash
npm test
```

页面使用 vinext 构建，并通过仓库内的 `.openai/hosting.json` 发布。
